// Local FastAPI serves the dashboard and API from the same origin.
// The Pages build replaces this file with its public backend configuration.
window.NBA_DEPLOYMENT = { mode: 'local', apiBaseUrl: '' };
